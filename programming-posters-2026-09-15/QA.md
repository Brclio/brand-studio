# 成品检查

检查完成日期：2026-09-16。目录按开始制作日期命名。

## 文件与生成方式

- 素材系列 18 张、Esther 系列 10 张，共 28 个独立 PNG。
- 所有图片原生尺寸为 `1672×941`，横版，接近 16:9。
- 28 个 SHA-256 全部不同；PNG 完整解码检查通过；总图片体积约 59.48 MiB。
- 图片直接从内置 imagegen 的原生输出复制入仓库；未后期叠字、裁切、拉伸或重编码。
- 每张图片的尺寸、字节数和哈希见 `manifest.json`、`SHA256SUMS.txt`。

## 文字与视觉

- 28 张均逐张肉眼查看了主体、构图、主标题、副标题和独立书名。
- 图片内书名为《编程启蒙：思维与代码》，封面参考来自本书实际素材。
- 10 张 Esther 海报保留“设计系统：ESTHER不二 · CC BY-NC-SA 4.0”署名。
- macOS Vision 只读 OCR 覆盖全部 28 张，原始结果保存在 `source/ocr-review.json`；OCR 并不替代肉眼判断。
- OCR 对素材 11 的 `Python`、Esther 03 的“落地”和 Esther 08 的 `Python` 有误识；已回看原图确认文字正确。
- 素材 10–16 和全部 Esther 海报由独立代理再检查，见 `source/visual-review.json`。
- 版式包含主视觉、非对称双篇、纸张路线、阶梯、知识陈列、巨数字、手绘流程与笔记批注。

## Esther 规范适配

- 按用户的横版要求适配卡片场景；配色按设计系统允许的品牌色替换规则调整。
- 使用暖奶油背景，深棕、金黄和少量朱红；真实黑金书封保留。
- 中文标题采用衬线视觉、辅助正文采用清晰简洁字体；大小对比明显。
- 通过手绘线条、角色与批注建立统一视觉；各图有不同版式。
- 生成图片中的颜色面积比例和字体为视觉近似，不宣称精确色彩测量或嵌入指定字体文件。
- 画廊基于提供的模板变量与风格适配，设置 900px / 600px 断点及 `clamp()` 字号，所有元素均有明确样式。

## 已知细节

- 书封是图像生成再现，并非把原封面像素无损贴入。部分极小作者字、宣传短句及手写出版社标识有软化或笔画近似；主体标题、独立书名及主要文案清晰。
- Esther 09 的打开书页带有一处类似书脊标题的边缘装饰，实体装帧细节略有艺术化。
- 素材 10–16 的原始提示词日志未在中断前保存，已按分派 brief 与成图补录设计说明，并明确区分原始记录；原生图片来源通过哈希匹配确认。

## 画廊

- 本地离线打开成功，28 张图片全部加载成功。
- 点击放大、方向键换图、Escape 关闭通过检查。
- 1440px 桌面与 390px 手机视口检查通过，无横向溢出。
- 未发现浏览器脚本错误。结果见 `source/gallery-review.json`。
